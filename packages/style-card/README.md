# @auteur/style-card

Compose measured and derived halves; version; confidence and card strength.

Layer: `service`. It may import packages in its own layer or below, never above.

## Dependencies

`@auteur/card-store`, `@auteur/core`, `@auteur/corpus-gutenberg`, `@auteur/errors`, `@auteur/ids`, `@auteur/prosody`

## Public surface

See `api-surface.md`, which CI regenerates and compares on every pull request.

## Testing

```sh
bun run turbo test --filter @auteur/style-card
```
