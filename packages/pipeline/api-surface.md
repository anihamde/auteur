# @auteur/pipeline — public API surface

Generated by `bun run fix:api-surface`. Do not edit by hand.

Every entry below is reachable by a consumer. A diff here is a contract
change and needs a contract review, per the implementation plan's gate 4.

## `@auteur/pipeline/clarify`

```ts
export const MAX_QUESTIONS = 8
export const MAX_ROUNDS = 3
export const applyBudget = (input: RoundInput): Round =>
export const clarifyQuestionSchema = z.object(
export const clarifyResultSchema = z.object(
export const parseClarifyResult = (payload: unknown): ClarifyResult =>
export const referencesAnswers = (
```

## `@auteur/pipeline/drift`

```ts
export const MATTR_MIN_WORDS = MATTR_WINDOW
export const measureDrift = (input: DriftInput): FitMeasure[] =>
```

## `@auteur/pipeline/engine`

```ts
export const runStage = async (input: RunStageInput): Promise<StageOutcome> =>
```

## `@auteur/pipeline/extract`

```ts
export const cardFromExtraction = (input: ExtractInput): StyleCard =>
export const extractedFieldSchema = z.object(
export const extractionSchema = z.object(
export const parseExtraction = (payload: unknown): Extraction =>
export const toEvidence = (
export const toExemplars = (
```

## `@auteur/pipeline/flush`

```ts
export const FLUSH_INTERVAL_MS = 250
export const createFlusher = (config: FlushConfig): Flusher =>
```

## `@auteur/pipeline/pins`

```ts
export const pinAll = (
export const requirePin = (input: ValidateInput): ModelDescriptor =>
export const validatePin = (input: ValidateInput): PinVerdict =>
```

## `@auteur/pipeline/pipeline`

```ts
export const findCycle = (pipeline: Pipeline): string[] | undefined =>
export const indexStages = (pipeline: Pipeline): StageIndex =>
export const upstreamOf = (
export const validatePipeline = (pipeline: Pipeline): Pipeline =>
```

## `@auteur/pipeline/resolve-tier`

```ts
export const requirementsFor = (
export const resolveAll = (
export const resolveTier = (input: ResolveInput): Resolution =>
```

## `@auteur/pipeline/sequential`

```ts
export const CARRY_WORDS = 500
export const joinScenes = (scenes: readonly string[]): string =>
export const planScenes = (beats: readonly Beat[]): ScenePlan[] =>
export const tailWords = (text: string, count = CARRY_WORDS): string =>
export const withContext = (
```

## `@auteur/pipeline/strategy`

```ts
export const INVOCATION_BUDGET_SECONDS = 240
export const SAFETY = 1.15
export const TOKENS_PER_SECOND = 40
export const TOKENS_PER_WORD = 1.4
export const estimateTokens = (words: number): number =>
export const resolveStrategy = (input: StrategyInput): StrategyDecision =>
```

## `@auteur/pipeline/usage`

```ts
export const accountFor = (
export const cancelledUsage = (
export const costOf = (
export const normalise = (
export const sessionCost = (stages: readonly StageUsage[]): number =>
```
