/** Screen 7 — the story, how well it matched, and what was decided for you. */
export const result = {
  /**
   * `ARCHITECTURE.md` §7.6's sentence, verbatim, on every story view and every
   * export. It is a structural rule rather than a caption: `export` takes the
   * label as a required parameter so a document cannot be rendered without it.
   */
  attribution:
    "Generated by auteur in the style of {author}. AI-generated text; not written by the author.",
  back: "Draft",
  decisionsAsked: "you answered",
  decisionsNotAsked: "model chose — not asked",
  decisionsSkipped: "model chose — question skipped",
  driftAsideLabel: "Draft against corpus",
  exportMarkdown: "Export markdown",
  newSession: "New session",
  reEnterable: "Every step is re-enterable from the rail.",
  regenerateSection: "Regenerate a section",
  tabs: {
    decisions: "Decisions log",
    fit: "Style fit",
    story: "Story",
  },
} as const;
