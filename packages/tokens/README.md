# @auteur/tokens

The design system's token CSS expressed as a Panda preset.

Layer: `foundation`. It may import packages in its own layer or below, never above.

## Dependencies

`@pandacss/dev`

## Public surface

See `api-surface.md`, which CI regenerates and compares on every pull request.

## Testing

```sh
bun run turbo test --filter @auteur/tokens
```
