# @auteur/style-fit

Bands, verdicts, and the deterministic half of the report.

Layer: `service`. It may import packages in its own layer or below, never above.

## Dependencies

`@auteur/core`, `@auteur/errors`, `@auteur/prosody`, `@auteur/style-card`

## Public surface

See `api-surface.md`, which CI regenerates and compares on every pull request.

## Testing

```sh
bun run turbo test --filter @auteur/style-fit
```
