# @auteur/text

Segmentation, Gutenberg cleaning and the cut ladder. Pure, versioned.

Layer: `foundation`. It may import packages in its own layer or below, never above.

## Dependencies

`@auteur/errors`

## Public surface

See `api-surface.md`, which CI regenerates and compares on every pull request.

## Testing

```sh
bun run turbo test --filter @auteur/text
```
